apiVersion: networking.k8s.io/v1
kind: Ingress

metadata:
  name: robotshop-ingress
  namespace: robotshop
  annotations:
    kubernetes.io/ingress.class: "gce"

spec:
  defaultBackend:
    service:
      name: rs-web
      port:
        number: 8080